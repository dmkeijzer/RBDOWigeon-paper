import scipy.optimize as sc_opt
import numpy as np
import BEM as BEM


# Variables to optimise: B, rpm hover, rpm cruise, delta_pitch hover, mayb even thrust factor
# Use % of the mission as weights


